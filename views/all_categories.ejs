<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>inventory</title>
</head>
<body>
<header>
    <%- include('./partials/nav'); %>
</header>
<main>
    <h1>All categories</h1>
    <ul>
        <% categories.forEach(function(category){ %>
            <li>
                <form method="post" action="/inventory/category/delete/<%= category._id %>">
                    <input type="hidden" name="item_id" value=<%= category._id %>/>
                    <div><%= category.category_name %></div>
                    <div><%= category.description %></div>
                    <button type="submit" formmethod="post" formaction="/inventory/category/delete/<%= category._id %>">
                        delete
                    </button>
                    <button type="submit" formmethod="get" formaction="/inventory/category/update/<%= category._id %>">
                        update
                    </button>
                </form>
            </li>
            <br>
        <% }); %>
    </ul>
</main>
</body>
</html>